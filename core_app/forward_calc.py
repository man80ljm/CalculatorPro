class ForwardCalculator:
    """正向计算占位实现，后续替换为 Step 1-5 逻辑"""

    def __init__(self, student_df, config):
        self.student_df = student_df
        self.config = config

    def run(self):
        raise NotImplementedError("正向计算逻辑尚未实现")
